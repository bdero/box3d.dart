/// The version of these Dart bindings, mirroring the package's pubspec version.
///
/// Once the native box3d library is vendored, a separate constant will report
/// the upstream box3d version the binaries were built from.
const String box3dBindingsVersion = '0.0.1';
